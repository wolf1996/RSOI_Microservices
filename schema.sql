DROP DATABASE IF EXISTS regsdb;
CREATE DATABASE regsdb;

DROP USER IF EXISTS regs_user;
CREATE USER regs_user WITH  PASSWORD '123456';

GRANT ALL PRIVILEGES ON DATABASE regsdb TO regs_user;

\c regsdb;
DROP TABLE IF EXISTS RSOI_REGS;
CREATE TABLE RSOI_REGS (
  ID       SERIAL PRIMARY KEY ,
  EVENT_ID INT,
  USER_ID VARCHAR(50)
);

INSERT INTO RSOI_REGS VALUES  (DEFAULT, 1,'simpleUser');
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO regs_user;
GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO regs_user;
