DROP TABLE IF EXISTS RSOI_REGS;
CREATE TABLE RSOI_REGS (
  ID       SERIAL PRIMARY KEY ,
  EVENT_ID INT,
  USER_ID VARCHAR(50)
);

INSERT INTO RSOI_REGS VALUES  (DEFAULT, 1,'s');
INSERT INTO RSOI_REGS VALUES  (DEFAULT, 1,'ss');