DROP DATABASE IF EXISTS eventsdb;
CREATE DATABASE eventsdb;

DROP USER IF EXISTS events_user;
CREATE USER events_user WITH  PASSWORD '123456';

GRANT ALL PRIVILEGES ON DATABASE eventsdb TO events_user;

\c eventsdb

DROP TABLE IF EXISTS EVENTS_INFO;
CREATE TABLE EVENTS_INFO (
  ID       SERIAL PRIMARY KEY ,
  USERNAME BIGINT,
  PARTICIPANT_COUNT INT DEFAULT(0),
  DESCRIPTION VARCHAR(1024)
);

INSERT INTO EVENTS_INFO VALUES  (DEFAULT, 1, DEFAULT , 'KISH concert');
INSERT INTO EVENTS_INFO VALUES  (DEFAULT, 1, DEFAULT , 'OP concert');
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO events_user;
